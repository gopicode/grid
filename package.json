{
  "name": "grid",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon --exec 'node --preserve-symlinks' -w src/ -w server.js server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
